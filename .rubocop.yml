inherit_gem:
  boxt_ruby_style_guide:
    - default.yml
    - pending.yml
    - rails.yml
    - rspec.yml

require:
  - rubocop-rails
  - rubocop-rspec

inherit_mode:
  merge:
    - Exclude
    - TargetRubyVersion

AllCops:
  TargetRubyVersion: 2.7
